{
  "$schema": "http://json-schema.org/draft-07/schema",
  "id": "api-lib",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?",
      "pattern": "^[a-zA-Z]{1}.*$"
    },
    "tags": {
      "type": "string",
      "description": "Add tags to the project (used for linting)",
      "alias": "t"
    },
    "directory": {
      "type": "string",
      "description": "A directory where the project is placed",
      "alias": "d"
    },
    "sourceSpecLib": {
      "type": "string",
      "description": "The workspace lib where the source OpenAPI spec file is located",
      "alias": "l",
      "x-prompt": "What's the name of the lib containing the API spec file?",
      "default": "api-spec"
    },
    "sourceSpecFileRelativePath": {
      "type": "string",
      "description": "The name of the source OpenAPI spec file, and its path relative to the root of the `sourceSpecLib` project",
      "alias": "f",
      "x-prompt": "What's the path of the API spec file, starting from the lib root?",
      "default": "src/api-spec.openapi.yml"
    },
    "generator": {
      "type": "string",
      "description": "The generator this lib will use to build an API SDK (e.g.: typescript-fetch)",
      "alias": "g",
      "x-prompt": "Which OpenAPITool generator would you like to use? (https://github.com/OpenAPITools/openapi-generator)",
      "default": "typescript-fetch"
    }
  },
  "required": ["name", "sourceSpecLib", "sourceSpecFileRelativePath", "generator"]
}
